// The markdown renderer now lives in @ssot/ui (shared with model-diagram +
// results-sheet). Re-exported here so existing `./Markdown` imports keep working;
// the implementation is byte-identical to what was here before.
export { Markdown } from "@ssot/ui/Markdown";
